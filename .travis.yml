language: node_js
node_js:
  - "8"
sudo: required
dist: trusty
services:
  - mongodb
  - redis-server
env:
  - NODE_ENV=ci
  - PORT=3000
cache:
  directories:
    - node_modules
    - client/node_modules
install:
  - npm install
  - npm run build
before_script:
  - nohup npm run start &
script:
  - npm run test
